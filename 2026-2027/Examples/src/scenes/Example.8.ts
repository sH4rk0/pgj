import { GameData } from "../GameData";
import Examples from "./Examples";

// Scena vuota/placeholder: struttura base di una scena Phaser, pronta per essere completata
export default class Example8 extends Examples {



  constructor() {
    super();
  }


  // create() viene chiamato una sola volta all'avvio della scena: qui si istanziano gli oggetti
  create() {


  }


  // update() viene chiamato ad ogni frame: qui va la logica che deve aggiornarsi continuamente
  update(time: number, delta: number): void {



  }



}
